{
  "name": "@canalnerd/scoring",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Motor de cálculo do score de popularidade do CanalNerd.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@canalnerd/core": "*"
  }
}
